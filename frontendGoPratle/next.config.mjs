/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove or comment out React Compiler config:
  // experimental: {
  //   reactCompiler: true,
  // },
  // Other config...
}

export default nextConfig